/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com','s3-alpha-sig.figma.com',"i0.wp.com","upload.wikimedia.org","res.cloudinary.com"],
      }
}

module.exports = nextConfig
